import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    marginTop: 16,
  },
  header: {
    fontWeight: 'bold',
    marginLeft: 16,
    fontSize: 20,
  },
});

export default styles;
