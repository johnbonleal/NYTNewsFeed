import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 4,
    backgroundColor: '#77cee7',
  },
  dropdownContainer: {
    flex: 0,
    flexDirection: 'row',
    alignItems: 'center',
    padding: 24,
    backgroundColor: '#75c1e2',
  },
});

export default styles;
